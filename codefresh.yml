version: '1.0'
steps:
  lint:
    image: quay.io/helmpack/chart-testing:latest
    working_directory: ${{main_clone}}
    description: Linting chart
    commands:
      - ct lint --config test/ct.yaml
